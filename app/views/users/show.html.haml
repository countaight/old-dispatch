- if current_user
	.success-message= flash[:success]
	%h2= @user.name
	%h4= @user.email
	- if current_user == @user
		= render "roles/#{@user.roles.first.name}_view"
- else
	.container
		%p.warning You are not logged in!
		%p.warning= link_to "Login", login_url